#ifndef Start
#define Start

#include <stdio.h>
#include <stdarg.h>
#include <math.h>
int sum_them_all(const unsigned int n, ...);
void print_numbers(const char *separator, const unsigned int n, ...);
#endif
