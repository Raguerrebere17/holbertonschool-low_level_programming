#include "main.h"

/**
 * _isupper - Returns 0 if c is uppercase, returns 0 otherwise
 * @c: A letter
 * Return: 1 if @c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	for (c = 'A'; c <= 'Z'; c++)
	{
	return (1);
	for (c = 'a'; c <= 'z'; c++)
	{
	return (0);
	}
	}
}
